name = "cloudflare-mcp"
main = "src/index.ts"
compatibility_date = "2025-05-01"
compatibility_flags = ["nodejs_compat"]

# Cloudflare-native Worker-to-Worker communication.
# The binding is already connected in the Cloudflare dashboard.
[[services]]
binding = "AI_IMAGES"
service = "ai-images-pilot"
